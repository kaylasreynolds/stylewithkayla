import{ApiError,dataResponse,withApi}from"@/lib/server/http";import{requireClientActionAccess}from"@/lib/server/profile-access";import{getD1}from"@/lib/server/runtime";type Params={params:Promise<{token:string}>};
export async function GET(_request:Request,ctx:Params){return withApi(async id=>{const{token}=await ctx.params,access=await requireClientActionAccess(token),row=await getD1().prepare(`SELECT b.status,b.public_reference AS publicReference,b.requested_start_at AS requestedStartAt,b.proposed_start_at AS proposedStartAt,b.proposed_end_at AS proposedEndAt,s.code AS serviceCode,s.name AS serviceName,c.full_name AS clientName FROM bookings b JOIN services s ON s.id=b.service_id JOIN clients c ON c.id=b.client_id WHERE b.id=?`).bind(access.bookingId).first<Record<string,unknown>>();if(!row||row.status!=="change_proposed")throw new ApiError(409,"ACTION_NO_LONGER_AVAILABLE","This alternate-time request is no longer active.");return dataResponse({booking:{...row,clientFirstName:String(row.clientName).trim().split(/\s+/)[0],clientName:undefined,requestedStartAt:new Date(row.requestedStartAt as number).toISOString(),proposedStartAt:new Date(row.proposedStartAt as number).toISOString(),proposedEndAt:new Date(row.proposedEndAt as number).toISOString()},expiresAt:new Date(access.expiresAt).toISOString()},200,id);});}
