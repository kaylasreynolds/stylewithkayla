import{requireAdmin}from"@/lib/server/admin-auth";import{sha256}from"@/lib/server/crypto";import{ApiError,dataResponse,optionalString,readJsonObject,rejectUnexpectedKeys,requiredString,validation,withApi}from"@/lib/server/http";import{getD1}from"@/lib/server/runtime";import{calculateAvailability,localDateKey}from"@/lib/server/scheduling";type Params={params:Promise<{bookingId:string;action:string}>};type B={id:string;status:string;serviceCode:string;profileType:string|null;clientId:string;clientEmail:string;holdId:string|null;holdStartsAt:number|null;holdEndsAt:number|null};
export async function POST(request:Request,ctx:Params){return withApi(async id=>{const admin=requireAdmin(request,true),{bookingId,action}=await ctx.params;if(!new Set(["confirm","decline","release-hold","propose-time"]).has(action))throw new ApiError(404,"ACTION_NOT_FOUND","Booking action not found.");const body=await readJsonObject(request);rejectUnexpectedKeys(body,["expectedStatus","reason","requestedStartAt","overrideConflict"]);const expected=requiredString(body.expectedStatus,"expectedStatus",40),db=getD1(),b=await load(db,bookingId);if(!b)throw new ApiError(404,"BOOKING_NOT_FOUND","Booking request not found.");if(b.status!==expected)throw new ApiError(409,"BOOKING_STATE_CHANGED","This request changed. Refresh before continuing.");const reason=optionalString(body.reason,"reason",500),now=Date.now(),keyHash=await sha256(request.headers.get("idempotency-key")||crypto.randomUUID());
 if(action==="confirm"){if(!b.holdId||b.holdStartsAt===null||b.holdEndsAt===null)throw new ApiError(409,"ACTIVE_HOLD_REQUIRED","Assign or propose a time before confirming.");if(!b.profileType)throw new ApiError(422,"PROFILE_TYPE_REQUIRED","Select a Style Profile type before confirming this request.");await db.batch([db.prepare(`UPDATE bookings SET status='confirmed',confirmed_start_at=?,confirmed_end_at=?,confirmed_at=?,updated_at=? WHERE id=? AND status=? AND EXISTS(SELECT 1 FROM booking_holds WHERE id=? AND active=1)`).bind(b.holdStartsAt,b.holdEndsAt,now,now,bookingId,expected,b.holdId),db.prepare(`UPDATE booking_holds SET kind='confirmed' WHERE id=? AND active=1 AND EXISTS(SELECT 1 FROM bookings WHERE id=? AND status='confirmed')`).bind(b.holdId,bookingId),db.prepare(`INSERT INTO style_profiles(id,booking_id,client_id,profile_type,status,schema_version,answers,current_section,created_at,updated_at) SELECT ?,id,client_id,profile_type,'draft',1,'{}',1,?,? FROM bookings WHERE id=? AND status='confirmed' ON CONFLICT(booking_id) DO NOTHING`).bind(crypto.randomUUID(),now,now,bookingId),history(db,bookingId,expected,"confirmed",admin.subjectId,reason,{action,idempotencyKeyHash:keyHash},now),communication(db,bookingId,b.clientEmail,"booking_confirmed",now)]);
 }else if(action==="decline"){const why=requiredString(body.reason,"reason",500);await db.batch([db.prepare(`UPDATE bookings SET status='declined',declined_at=?,updated_at=? WHERE id=? AND status=?`).bind(now,now,bookingId,expected),db.prepare(`UPDATE booking_holds SET active=0,released_at=?,release_reason=? WHERE booking_id=? AND active=1 AND EXISTS(SELECT 1 FROM bookings WHERE id=? AND status='declined')`).bind(now,why,bookingId,bookingId),history(db,bookingId,expected,"declined",admin.subjectId,why,{action,idempotencyKeyHash:keyHash},now),communication(db,bookingId,b.clientEmail,"booking_declined",now)]);
 }else if(action==="release-hold"){if(!b.holdId)throw new ApiError(409,"NO_ACTIVE_HOLD","This request no longer has an active hold.");const why=reason??"Released by Kayla";await db.batch([db.prepare(`UPDATE booking_holds SET active=0,released_at=?,release_reason=? WHERE id=? AND active=1`).bind(now,why,b.holdId),history(db,bookingId,expected,expected,admin.subjectId,why,{action,idempotencyKeyHash:keyHash},now)]);
 }else{const text=requiredString(body.requestedStartAt,"requestedStartAt",40),start=Date.parse(text);if(!Number.isFinite(start)||new Date(start).toISOString()!==text)throw validation("requestedStartAt","Choose a valid available time.");const date=localDateKey(start,"America/Boise"),availability=await calculateAvailability(db,b.serviceCode,date,date,Date.now(),bookingId),slot=availability.slots.find(x=>x.startsAt===text);if(!slot)throw new ApiError(409,"SLOT_UNAVAILABLE","That alternate time is unavailable.");const end=Date.parse(slot.endsAt),holdId=crypto.randomUUID();await db.batch([db.prepare(`UPDATE booking_holds SET active=0,released_at=?,release_reason='Replaced by proposed time' WHERE booking_id=? AND active=1`).bind(now,bookingId),db.prepare(`INSERT INTO booking_holds(id,booking_id,kind,starts_at,ends_at,active,created_at) SELECT ?,id,'proposed',?,?,1,? FROM bookings WHERE id=? AND status=? AND NOT EXISTS(SELECT 1 FROM booking_holds h WHERE h.active=1 AND h.booking_id<>? AND h.starts_at<? AND h.ends_at>?)`).bind(holdId,start,end,now,bookingId,expected,bookingId,end,start),db.prepare(`UPDATE bookings SET status='change_proposed',proposed_start_at=?,proposed_end_at=?,updated_at=? WHERE id=? AND status=? AND EXISTS(SELECT 1 FROM booking_holds WHERE id=? AND active=1)`).bind(start,end,now,bookingId,expected,holdId),history(db,bookingId,expected,"change_proposed",admin.subjectId,reason,{action,idempotencyKeyHash:keyHash},now),communication(db,bookingId,b.clientEmail,"alternate_time_proposed",now)]);}
 const updated=await load(db,bookingId);if(!updated||(action!=="release-hold"&&updated.status===expected))throw new ApiError(409,"BOOKING_STATE_CHANGED","This request changed. Refresh before continuing.");return dataResponse({bookingId,status:updated.status,holdActive:Boolean(updated.holdId)},200,id);});}
async function load(db:D1Database,id:string){return db.prepare(`SELECT b.id,b.status,b.profile_type AS profileType,b.client_id AS clientId,s.code AS serviceCode,c.email AS clientEmail,h.id AS holdId,h.starts_at AS holdStartsAt,h.ends_at AS holdEndsAt FROM bookings b JOIN services s ON s.id=b.service_id JOIN clients c ON c.id=b.client_id LEFT JOIN booking_holds h ON h.booking_id=b.id AND h.active=1 WHERE b.id=?`).bind(id).first<B>();}
function history(db:D1Database,id:string,from:string,to:string,actor:string,reason:string|null,metadata:object,now:number){return db.prepare(`INSERT INTO booking_status_history(id,booking_id,from_status,to_status,actor_type,actor_id,reason,metadata,created_at) SELECT ?,id,?,?,'admin',?,?,?,? FROM bookings WHERE id=? AND status=?`).bind(crypto.randomUUID(),from,to,actor,reason,JSON.stringify(metadata),now,id,to);}
function communication(db:D1Database,id:string,email:string,key:string,now:number){return db.prepare(`INSERT INTO communications(id,booking_id,channel,template_key,recipient,status,metadata,created_at) SELECT ?,id,'email',?,?,'recorded',?,? FROM bookings WHERE id=?`).bind(crypto.randomUUID(),key,email,JSON.stringify({deliveryDeferred:true}),now,id);}
