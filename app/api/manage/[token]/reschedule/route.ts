import { ApiError,dataResponse,optionalString,readJsonObject,rejectUnexpectedKeys,requiredString,validation,withApi } from "@/lib/server/http";
import { requireManageAccess } from "@/lib/server/profile-access";import{getD1}from"@/lib/server/runtime";
type Params={params:Promise<{token:string}>};
export async function POST(request:Request,ctx:Params){return withApi(async id=>{const{token}=await ctx.params,access=await requireManageAccess(token),body=await readJsonObject(request);rejectUnexpectedKeys(body,["requestedStartAt","note"]);const raw=requiredString(body.requestedStartAt,"requestedStartAt",40),start=Date.parse(raw);if(!Number.isFinite(start)||start<=Date.now())throw validation("requestedStartAt","Choose a future appointment time.");const note=optionalString(body.note,"note",1000),db=getD1(),booking=await db.prepare(`SELECT status,confirmed_start_at AS startsAt,confirmed_end_at AS endsAt FROM bookings WHERE id=?`).bind(access.bookingId).first<{status:string;startsAt:number;endsAt:number}>();if(!booking||booking.status!=="confirmed")throw new ApiError(409,"APPOINTMENT_NOT_CONFIRMED","This appointment can no longer be rescheduled.");const duration=booking.endsAt-booking.startsAt,now=Date.now();await db.batch([db.prepare(`UPDATE reschedule_requests SET status='declined',reviewed_at=? WHERE booking_id=? AND status='pending'`).bind(now,access.bookingId),db.prepare(`INSERT INTO reschedule_requests(id,booking_id,requested_start_at,requested_end_at,note,status,created_at) VALUES(?,?,?,?,?,'pending',?)`).bind(crypto.randomUUID(),access.bookingId,start,start+duration,note,now),db.prepare(`INSERT INTO booking_status_history(id,booking_id,from_status,to_status,actor_type,reason,metadata,created_at) VALUES(?,?,'confirmed','confirmed','client',?,?,?)`).bind(crypto.randomUUID(),access.bookingId,note,JSON.stringify({action:"request_reschedule",requestedStartAt:new Date(start).toISOString()}),now)]);return dataResponse({message:"Request received. Your current appointment remains confirmed until Kayla approves the new time."},201,id);});}
