"use client";import{useEffect,useState}from"react";import Link from"next/link";
type Booking={clientFirstName:string;serviceName:string;serviceCode:string;requestedStartAt:string;proposedStartAt:string;proposedEndAt:string};type Slot={startsAt:string;endsAt:string};
export default function ClientActionsPage(){return <ClientActionClient/>}export function ClientActionClient({token}:{token?:string}){const[booking,setBooking]=useState<Booking|null>(null),[error,setError]=useState(""),[loading,setLoading]=useState(Boolean(token)),[mode,setMode]=useState<"choice"|"another"|"confirmed"|"pending">("choice"),[slots,setSlots]=useState<Slot[]>([]),[selected,setSelected]=useState(""),[profileLink,setProfileLink]=useState(""),[working,setWorking]=useState(false);
 useEffect(()=>{if(!token)return;fetch(`/api/client-actions/${token}`,{cache:"no-store",referrerPolicy:"no-referrer"}).then(async response=>{const payload=await response.json() as {data?:{booking:Booking};error?:{message?:string}};if(!response.ok)throw new Error(payload.error?.message||"This private link is unavailable.");return payload.data!.booking;}).then(setBooking).catch(current=>setError(current instanceof Error?current.message:"This private link is unavailable.")).finally(()=>setLoading(false));},[token]);
 async function accept(){if(!token)return;setWorking(true);setError("");try{const response=await fetch(`/api/client-actions/${token}/accept-proposed-time`,{method:"POST",headers:{"idempotency-key":crypto.randomUUID()},referrerPolicy:"no-referrer"}),payload=await response.json() as {data?:{profileAccessUrl:string};error?:{message?:string}};if(!response.ok)throw new Error(payload.error?.message||"The proposed time could not be accepted.");setProfileLink(payload.data!.profileAccessUrl);setMode("confirmed");}catch(current){setError(current instanceof Error?current.message:"The proposed time could not be accepted.");}finally{setWorking(false);}}
 async function chooseAnother(){if(!booking)return;setMode("another");setWorking(true);setError("");try{const from=dateKey(new Date()),end=new Date();end.setDate(end.getDate()+60);const response=await fetch(`/api/availability?serviceCode=${encodeURIComponent(booking.serviceCode)}&from=${from}&to=${dateKey(end)}`,{cache:"no-store"}),payload=await response.json() as {data?:{slots:Slot[]};error?:{message?:string}};if(!response.ok)throw new Error(payload.error?.message||"Availability could not be loaded.");setSlots(payload.data!.slots);setSelected(payload.data!.slots[0]?.startsAt||"");}catch(current){setError(current instanceof Error?current.message:"Availability could not be loaded.");}finally{setWorking(false);}}
 async function requestAnother(){if(!token||!selected)return;setWorking(true);setError("");try{const response=await fetch(`/api/client-actions/${token}/request-another-time`,{method:"POST",headers:{"content-type":"application/json","idempotency-key":crypto.randomUUID()},referrerPolicy:"no-referrer",body:JSON.stringify({requestedStartAt:selected})}),payload=await response.json() as {error?:{message?:string}};if(!response.ok)throw new Error(payload.error?.message||"The new time could not be requested.");setMode("pending");}catch(current){setError(current instanceof Error?current.message:"The new time could not be requested.");}finally{setWorking(false);}}
 if(!token)return <ActionShell><h1>Private link required.</h1><p>Please use the alternate-time link from Kayla.</p></ActionShell>;if(loading)return <ActionShell><p>Loading your appointment options…</p></ActionShell>;if(!booking)return <ActionShell><h1>Link unavailable.</h1><p>{error}</p></ActionShell>;if(mode==="confirmed")return <ActionShell><p className="eyebrow">APPOINTMENT CONFIRMED</p><h1>That time is yours.</h1><p>Your appointment is confirmed for {format(booking.proposedStartAt)}.</p><Link className="button primary-button" href={profileLink}>Complete your Style Profile</Link></ActionShell>;if(mode==="pending")return <ActionShell><p className="eyebrow">NEW TIME REQUESTED</p><h1>Thank you.</h1><p>Your new selection is pending while Kayla reviews it. The time is held in the meantime.</p></ActionShell>;
 return <ActionShell><p className="eyebrow">APPOINTMENT TIME</p><h1>Hi, {booking.clientFirstName}.</h1><p>Kayla proposed a different time for your {booking.serviceName} appointment.</p><div className="proposed-time"><span>PROPOSED TIME</span><strong>{format(booking.proposedStartAt)}</strong><p>Macy&apos;s Boise Towne Square</p></div>{error&&<p className="booking-error" role="alert">{error}</p>}{mode==="another"?<div className="another-time"><label>Choose another available time<select value={selected} onChange={event=>setSelected(event.target.value)}>{slots.map(slot=><option value={slot.startsAt} key={slot.startsAt}>{format(slot.startsAt)}</option>)}</select></label>{!working&&!slots.length&&<p>No routine times are currently available. Please contact Kayla.</p>}<div><button className="text-button" onClick={()=>setMode("choice")}>Back</button><button className="button primary-button" disabled={!selected||working} onClick={()=>void requestAnother()}>Request this time</button></div></div>:<div className="client-action-buttons"><button className="button primary-button" disabled={working} onClick={()=>void accept()}>Accept proposed time</button><button className="button secondary-button" disabled={working} onClick={()=>void chooseAnother()}>Request another time</button></div>}</ActionShell>}
function ActionShell({children}:{children:React.ReactNode}){return <div className="site-shell"><header className="site-header"><div className="container header-inner"><Link className="site-logo" href="/"><img src="/images/stylewithkayla_logo.png" alt="Style with Kayla"/></Link></div></header><main className="client-action-page">{children}</main></div>}function format(value:string){return new Intl.DateTimeFormat("en-US",{timeZone:"America/Boise",weekday:"long",month:"long",day:"numeric",year:"numeric",hour:"numeric",minute:"2-digit"}).format(new Date(value))}function dateKey(value:Date){const parts=new Intl.DateTimeFormat("en-CA",{timeZone:"America/Boise",year:"numeric",month:"2-digit",day:"2-digit"}).formatToParts(value),part=(type:string)=>parts.find(item=>item.type===type)?.value;return`${part("year")}-${part("month")}-${part("day")}`;}
