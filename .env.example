# Server-side runtime bindings only. Do not expose these through NEXT_PUBLIC_* variables.
ADMIN_EMAILS=
MAINTENANCE_SECRET=
# Base64url-encoded 32-byte server-side key (for example: openssl rand -base64 32 | tr '+/' '-_' | tr -d '=')
STYLE_SUMMARY_TOKEN_ENCRYPTION_KEY=
